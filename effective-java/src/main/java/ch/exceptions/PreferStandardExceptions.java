package ch.exceptions;

import com.google.common.collect.Ranges;

/* Item 60
 * IllegalArgumentException is the most commonly reused exception
 * IllegalArgumentException is used when receiving object is in the wrong state to receive it.
 * NullPointerException when null is prohibited
 * IndexOutOfBounds when element is out of range
 * ConcurrentModificationException when an operation that isn't threadsafe detected
 * UnsupportedOperationException use when an implementation doesn't support an optional method
 */
public class PreferStandardExceptions {
	


}
